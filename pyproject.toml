[project]
name = "it-support-env"
version = "0.1.0"
description = "IT Support Troubleshooting Environment"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv-core"
]

[project.scripts]
server = "server.app:main"